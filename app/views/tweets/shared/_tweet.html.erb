<%= link_to tweet_path(tweet), class: "tweet_item" do %>
  <div class="p-8">
    <div class="mb-4">
      <p class="text-lg font-semibold text-gray-900">@<%= tweet.user.username %></p>
      <p class="text-sm text-gray-600"><%= tweet.created_at.to_formatted_s(:long) %></p>
    </div>
    <p class="tweet_body"><%= format_body(tweet.body) %></p>

    <%= render "tweets/shared/tweet_controls", tweet: tweet %>
  </div>
<% end %>
