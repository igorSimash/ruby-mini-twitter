<div class="block max-w-xl mx-auto bg-white shadow-md rounded-xl md:max-w-3xl">
  <div class="px-4 py-2 border-b">
    <p class="text-sm">
      @<%= tweet.user.username %> reposted
    </p>
  </div>

  <%= render "shared/tweets/basic_tweet", tweet: tweet.origin %>
</div>
